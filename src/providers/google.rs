//! Google Sign-In identity provider — id_token (JWT) verification.
//!
//! Implementation lands in subsequent commits (verifier scaffold, JWT validation,
//! JWKS cache). This file exists so `--features google` builds cleanly.
